syntax = "proto3";

package xBase;

option go_package = ".;xGrpcGenerate";
option java_package = "com.frontleaves.phalanx.beacon.sso.sdk.grpc.base";
option java_multiple_files = true;
option java_outer_classname = "BaseProto";

// BaseResponse 表示 gRPC 统一响应的元信息部分。
message BaseResponse {
  // 请求追踪标识（。
  string context = 1;
  // 输出标识（如 "Success"、"PARAMETER_ERROR"）。
  string output = 2;
  // 业务状态码（200 表示成功，其余与 xError.ErrorCode.Code 对应）。
  uint64 code = 3;
  // 人类可读的描述信息。
  string message = 4;
  // 补充性错误详情，仅在错误场景下填充。
  optional string error_message = 5;
  // 请求处理耗时（微秒），仅在调试模式下填充。
  optional int64 overhead = 6;
}
